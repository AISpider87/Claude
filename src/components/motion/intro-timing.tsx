/**
 * Timing of the sign-in cinematic, shared by the overlay (which owns the
 * timers) and the stage (which owns the choreography), in seconds.
 */
export const INTRO_TRAVEL = 0.5;
export const INTRO_IMPACT = 0.56;
export const INTRO_EMBLEM = 0.66;
export const INTRO_WORD = 0.92;
export const INTRO_WIPE = 1.12;
export const INTRO_END = 1.45;

/** Reduced motion: one short fade of the emblem instead of the whole thing. */
export const INTRO_REDUCED_END = 0.2;
